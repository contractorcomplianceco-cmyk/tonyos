kind = "web"
previewPath = "/"
title = "Executive Strategy Cockpit"
version = "1.0.0"
id = "artifacts/cockpit"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22274

[services.development]
run = "pnpm --filter @workspace/cockpit run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cockpit", "run", "build" ]
publicDir = "artifacts/cockpit/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22274"
BASE_PATH = "/"
